import bcrypt from "bcrypt";
const round = 16;

export default function signUp(
  fullName: String,
  email: String,
  password: String
) {
  // Checck if user exist in the database
  fullName = "Nathan";
  email = "nathan";
  password = "1234";
}
